package queryExecutor;

/**
 * Detects and loads meta-data of compressed predicate files (block mode)
 * @author UTILISATEUR
 *
 */
public class MetaLoader {
	
	public static BTree load(String predpath){
		BTree loaded = new BTree();
		return loaded;
	}

}
